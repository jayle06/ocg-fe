import { createStore } from 'vuex'
import products from "./modules/products.module";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    products,
  }
})
